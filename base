,mcd() {
  : ,mcd DIRECTORY
  : Create a directory and move inside it. Nested directories are valid
  if [[ -z $1 ]]; then
    echo "Missing directory name"
    return 1
  else
    mkdir -p "$1"
    cd "$1"
  fi
}